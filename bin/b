#!/usr/bin/env bash

function set_terminal_profile () {
  echo "tell application \"Terminal\" to set current settings of first window to settings set named \"$1\"" | osascript
}

on_exit () {
  set_terminal_profile "Pro"
}
trap on_exit EXIT

host="192.168.70.1$1"
args="${@:2}"

vpn-connect.sh
set_terminal_profile "Man Page"
ssh "root@$host" "$args"