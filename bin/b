#!/usr/bin/env bash

function set_terminal_profile () {
  echo -e "\033]50;SetProfile=$1\a"
}

on_exit () {
  set_terminal_profile "Default"
}
trap on_exit EXIT

args="${@:2}"

vpn-connect.sh $1
set_terminal_profile "Prod"
ssh -t "root@ffb$1" "$args"
